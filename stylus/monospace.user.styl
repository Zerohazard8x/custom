/* ==UserStyle==
@name           monospace
@namespace      github.com/openstyles/stylus
@version        1.2.8
@description    My personal changes to codeblocks, etc
@author         Zerohazard8x + LLM
@updateURL      https://github.com/Zerohazard8x/custom/raw/main/stylus/monospace.user.styl
@homepageURL    https://github.com/Zerohazard8x/custom/tree/main/stylus
==/UserStyle== */

@import url("https://fonts.googleapis.com/css?family=Inter|Roboto|Noto+Sans+JP|Noto+Sans+TC|Noto+Sans+SC|Noto+Sans+KR|Fira+Code|IBM+Plex+Mono|Libre+Baskerville");

:root {
    /* Define variables */
    --not-monospace: -apple-system, BlinkMacSystemFont, "SF Pro", "SF Pro Text",
        "Inter", "Roboto", "IBM Plex Sans", "Noto Sans", "Noto Sans JP",
        "Noto Sans TC", "Noto Sans SC", "Noto Sans KR", "IBM Plex Sans JP",
        "IBM Plex Sans KR";
	--serif: "Libre Baskerville", "EB Garamond";
    --monospace: "Fira Code", "Fira Mono", "Noto Mono", "Noto Sans Mono",
        "DroidSansM", "SF Mono", "SFMono-Regular", "CodeNewRoman Nerd Font",
        "Consolas", "Hack", "Hack Nerd Font", "Bitstream Vera Sans Mono",
        "BitstromWera Nerd Font", "DejaVu Sans Mono", "Cascadia Code",
        "CaskaydiaCove Nerd Font", "Cascadia Mono", "Segoe UI Mono",
        "Intel One Mono", Menlo, "MesloLG Nerd Font", Monaco, "Ubuntu Mono",
        "Ubuntu Mono Nerd Font", "Hasklig", "Hasklug Nerd Font",
        "Source Code Pro", "Source Code Pro Nerd Font", "Andale Mono",
        "Lucida Console", "Lucida Sans Typewriter";
    --block-mono: "IBM Plex Mono", "BlexMono Nerd Font", "Roboto Mono",
        "Fantasque Sans Mono", "FantasqueSansM Nerd Font", "Agave Nerd Font",
        "JetBrains Mono", "JetBrains Mono NL", "JetBrainsMono Nerd Font",
        ProFont, "Input Mono", "Envy Code R", "EnvyCodeR Nerd Font",
        "Iosevka Expanded", "Iosevka", "Iosevka Nerd Font",
        "IosevkaTerm Nerd Font", "Input Sans", "Liberation Mono";
    --mono-combo: var(--block-mono), var(--monospace);
}

@-moz-document domain("github.com") {
    /* Github - Change all fonts, change font and formatting of code blocks, grayscale */
    .Layout,
    .markdown-body,
    .page-responsive,
    .PageHeader-title,
    .Box-sc-g0xbh4-0,
    .Dialog__Footer-sc-uaxjsn-6,
    .my-3,
    .d-block,
    .container,
    section,
    details,
    blockquote,
    .container-lg {
        font-family: var(--not-monospace), -apple-system, BlinkMacSystemFont,
            "Segoe UI", "Noto Sans", Helvetica, Arial, sans-serif,
            "Apple Color Emoji", "Segoe UI Emoji" !important;
    }

    tt,
    #commit-summary-input,
    .blob-code-inner,
    .blob-num,
    .branch-name,
    .diffstat,
    .file-info,
    .sha,
    .sha-block,
    .text-mono,
    .input-monospace,
    .BranchName-sc-sg8jsy-0,
    pre,
    kbd,
    code {
        font-family: var(--mono-combo), ui-monospace, SFMono-Regular, SF Mono,
            Menlo, Consolas, Liberation Mono, monospace !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;

        /* -webkit-font-feature-settings: "liga", "calt" !important;
        -moz-font-feature-settings: "liga", "calt" !important;
        -o-font-feature-settings: "liga", "calt" !important; */
    }

    .blob-code-inner,
    .blob-num,
    pre {
        font-size: 16px !important;
    }

    div.logged-in.env-production.page-responsive.full-width,
    div.logged-in.env-production.page-responsive,
    div.sr-only {
        filter: grayscale(80%);
    }
}

@-moz-document domain("wikipedia.org") {
    /* Wikipedia - Replace Arial font, Codeblocks, grayscale */
    #searchform,
    #p-lang-btn-label,
    #vector-page-tools-dropdown-label,
    .grid,
    .noime,
    .invite,
    .vector-body,
    .vector-menu,
    .vector-menu-content-list,
    .vector-pinnable-header-toggle-button,
    .vector-toc,
    cite,
    .mwe-popups-container {
        font-family: var(--not-monospace), sans-serif !important;
    }

    p > code,
    li > code,
    pre > span {
        font-family: var(--mono-combo), monospace !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    #p-namespaces,
    a.mw-jump-link,
    div.mw-page-container,
    div.mwe-popups.mwe-popups-type-page,
    div.vector-header-container,
    div.vector-settings,
    a.oo-ui-element-hidden {
        filter: grayscale(80%);
    }
}

@-moz-document domain("reddit.com") {
    /* Reddit - Code blocks, fonts, UI edits */
    .loggedin {
        font-family: Noto Sans, verdana, arial, helvetica, sans-serif !important;
    }

    #header,
    .head {
        font-family: IBMPlexSans, Noto Sans, verdana, arial, helvetica,
            sans-serif !important;
    }

    .title {
        font-family: IBMPlexSans, arial, verdana, arial, helvetica, sans-serif !important;
    }

    ._3oHXlwUZwpR5SS-8P9tILX {
        font-family: IBMPlexSans, NotoSans, Arial, sans-serif !important;
    }

    ._34q3PgLsx9zIU5BiSOjFoM {
        font-family: var(--mono-combo), Noto Mono, Monaco, Consolas, monospace !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }
	
	pre > ._34q3PgLsx9zIU5BiSOjFoM {
        font-size: 16px !important;
    }

    /* UI */
    .h-jI8r2f9ozTNqu_2TBeY {
        width: 1px !important;
    }

    ._3jiriKeNer8y0-1r6oWIFM {
        width: 1px !important;
    }

    ._3jiriKeNer8y0-1r6oWIFM {
        min-width: 40px !important;
    }
}

@-moz-document domain("stackoverflow.com"), domain("superuser.com"), domain("stackexchange.com") {
    /* StackOverflow - All fonts, grayscale */
    code {
        font-family: var(--mono-combo), ui-monospace, "Cascadia Mono",
            "Segoe UI Mono", "Liberation Mono", Menlo, Monaco, Consolas,
            monospace !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

	.dno,
    .fw-bold,
    .unified-theme,
    .question-hyperlink,
    .comment-copy,
    .s-prose {
        font-family: var(--not-monospace), -apple-system, BlinkMacSystemFont,
            "Segoe UI Adjusted", "Segoe UI", "Liberation Sans", sans-serif !important;
    }

    /* Monospace */
    #notify-container,
    #custom-header,
    header.s-topbar.ps-fixed.t0.l0.js-top-bar,
    div.container,
    footer.site-footer.js-footer,
    div.d-none,
    #onetrust-banner-sdk,
    div.d-none.js-consent-banner-version,
    header.site-header {
        filter: grayscale(80%);
    }
}

@-moz-document domain("poe.com") {
    /* Poe - Reduce usage of system font, grayscale */
    .PageWithSidebarLayout_centeredPage__D42BU,
    .ReactModal__Content {
        font-family: var(--not-monospace), apple-system, system-ui,
            BlinkMacSystemFont, Segoe UI, Roboto, Oxygen-Sans, Ubuntu, Cantarell,
            Helvetica Neue, sans-serif !important;
    }

    code > span {
        font-family: var(--mono-combo), Consolas, monaco, monospace !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    #__next,
    div.ReactModalPortal {
        filter: grayscale(80%);
    }
}

@-moz-document domain("chat.openai.com") {
    /* ChatGPT - Code blocks, grayscale */
    code {
        font-family: var(--mono-combo), Söhne Mono, Monaco, Andale Mono,
            Ubuntu Mono, monospace !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    #__next {
        filter: grayscale(80%);
    }
}

@-moz-document domain("w3schools.com") {
    /* W3Schools - Code blocks, grayscale */
    .w3-code,
    .language-java,
    .w3-codespan {
        font-family: var(--mono-combo), Consolas, "Source Code Pro", Menlo,
            Monaco, "Andale Mono", "Ubuntu Mono", monospace !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    div.w3-bar.notranslate.w3-white,
    div.w3-hide-large,
    nav.w3-hide-small.navex,
    div.topnav.notranslate,
    div.w3-sidebar.w3-collapse,
    div.w3-main.w3-light-grey,
    a.ga-bottom,
    #pagetual-preload {
        filter: grayscale(80%);
    }
}

@-moz-document domain("learn.microsoft.com") {
    /* MS Learn - Code blocks, grayscale */
    pre > code,
    p > code,
    li > code {
        font-family: var(--mono-combo), SFMono-Regular, Consolas,
            Liberation Mono, Menlo, Courier, monospace !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    div.header-holder.has-default-focus,
    div.mainContainer.uhf-container.uhf-container.is-full.has-default-focus,
    section.border-top.has-default-focus.is-hidden-interactive.margin-top-sm.margin-top-none,
    div.action-panel.has-default-focus {
        filter: grayscale(80%);
    }
}

@-moz-document domain("wikiwand.com") {
    /* Wikiwand - Code blocks, serif, grayscale */
    .monospaced,
    code,
    pre {
        font-family: var(--mono-combo), Noto Sans Mono, Menlo, Consolas,
            Courier New, Courier, monospace !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }
	
	.monospaced,
    pre {
        font-size: 16px !important;
    }
	
	.section-h, .item_item__Faryz {
		font-family: var(--serif), Noto Serif, "Noto Serif", "Noto Serif-fallback-1", serif !important;
	}
	
	.gallery_wrapper__1a7bM,
	article {
		filter: grayscale(80%);
	}
}

@-moz-document domain("developer.mozilla.org") {
    /* Mozilla Dev - Code blocks, grayscale */
    code,
    .section-content > pre {
        font-family: var(--mono-combo), monospace !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    pre > code,
    p > code,
    strong > code {
        font-size: 16px !important;
    }

    #root,
    #a11y-status-message {
        filter: grayscale(80%);
    }
}

@-moz-document domain("symbolab.com") {
    /* Symbolab - LaTeX input interface, grayscale */
    .latex-command-input {
        font-family: var(--mono-combo), "Courier New", monospace !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    #dateTimePicker,
    #fb-root,
    #force-svg,
    #hiddenData,
    #loading-spinner,
    #no-render-svg,
    #symbolabLogoPrint,
    #symbolabLogoPrint1,
    div.hide-important,
    div.nl-cookiepolicy,
    div.nl-footer.print-hide.no-select,
    div.nl-generatePdf.nl-hidden,
    div.nl-hidden.nl-generatePdf,
    div.nl-page,
    div.print-hide,
    section.nl-center,
    div.nl-hidden.page {
        filter: grayscale(80%);
    }
}

@-moz-document domain("tcrf.net"), domain("mariowiki.com"), domain("ssbwiki.com"), domain("wikirby.com"), domain("splatoonwiki.org"), domain("niwanetwork.org"), domain("betawiki.net") {
    /* MediaWiki - Reduce usage of Arial font, code blocks, grayscale */
    .mediawiki,
    #sidebar,
    h3 {
        font-family: var(--not-monospace), sans-serif !important;
    }

    input {
        font-family: var(--not-monospace), "MS Shell Dlg 2" !important;
    }

    .pBody {
        font-family: var(--not-monospace), verdana, sans-serif !important;
    }

    #personal > h2 {
        font-family: var(--not-monospace), "Helvetica Neue", "Nimbus Sans",
            "Helvetica", "Arial", sans-serif !important;
    }

    .mp-box > p > code,
    tt,
    .mw-parser-output > pre {
        font-family: var(--mono-combo), monospace !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    #mw-head,
    #mw-wrapper,
    #globalWrapper,
    a.mw-wiki-logo,
    div.mw-panel,
    div.noprint,
    div.mw-body,
    #footer,
    div.suggestions {
        filter: grayscale(80%);
    }
}

@-moz-document domain("bard.google.com") {
    /* Google bard - code blocks */
    .code-container,
    .markdown > p code {
        font-family: "Google Sans Mono", var(--mono-combo), monospace !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }
}

@-moz-document domain("bing.com") {
    /* Bing - Make sure website uses one single font + "fluent" changes + code blocks + grayscale */
    :root {
        --roboto-then-segoe-text: "Roboto", "Segoe UI Variable Text", "Segoe UI",
            SegoeUI, Segoe;
        --segoe-display: "Segoe UI Variable Display", "Segoe UI", SegoeUI, Segoe,
            "Roboto";
        --roboto-then-segoe-small: "Roboto", "Segoe UI Variable Small",
            "Segoe UI", SegoeUI, Segoe;
    }

    /* Search box */
    .b_searchboxForm {
        border-radius: 12px !important;
    }

    /* Navigation bar */
    .b_scopebar > ul li {
        text-transform: none !important;
        font-size: 14px !important;
        font-family: var(--roboto-then-segoe-text), Arial, Helvetica, sans-serif !important;
    }

    .b_scopebar_item > a {
        text-transform: none !important;
        font-size: 13px !important;
    }

    /* Grayscale filter */
	cib-serp,
	.sb_count,
    .sbiopc,
    .ptitle,
    .ent-dtab-btn,
    .scs_theme,
    .b_imagePair,
    #syd_qna_suggestion,
    .rs_chatReD,
    .b_algo,
    .b_sideBleed,
    .mc_vtvc_meta_block,
    .b_scopebar,
    #fltIdtCon,
    #imagemeta,
    #rd_gtrtb_ttl,
    #vvlb,
    .algoSlug_icon,
    .asSectionHeading,
    .b_algo > h2 > a,
    .b_algospacing,
    .b_app_common_btn,
    .b_bfbLcgAnchorText,
    .b_entityTitle,
    .b_fbText,
    .b_focusTextLarge,
    .b_focusTextMedium,
    .b_logo,
    .b_mgridTitle,
    .b_primtxt,
    .b_rc_gb_widget_title,
    .b_seeMoreCont,
    .b_title,
    .b_topTitle,
    .b_vList,
    .b_vPanel,
    .cbtn,
    .container,
    .dest_name,
    .dg_b .infnmpt .infpd a,
    .ent-dtab-nam-ov-dtp,
    .fdbtext,
    .hb_title_col,
    .hp_body,
    .irp-video-slogan,
    .item,
    .l_ecrd_ftr_txt,
    .l_ecrd_rrct-items,
    .mc_vtvc_meta_row_channel,
    .mc_vtvc_title > span,
    .mc_vtvc_title > strong,
    .mc_vtvc_title,
    .metaItem,
    .ms-Button-label,
    .ms-search-tenantLogo-text,
    .ms-search-text-h2,
    .qna_algoLink_title,
    .rd_cCodeBg,
    .rd_code_blu,
    .rd_code_red,
    .rd_gtrtb_nav_item,
    .recommendationsTableTitle > h2,
    .rstc,
    .rwrl,
    .sa_tm_subText,
    .sb_f_count,
    .scopes,
    .scs_metactn > div h2,
    .sto_attr_header,
    .sto_attr_table,
    .sto_attrlitc,
    .sto_title,
    .tdw_button,
    .tit,
    .title,
    .tptxt,
    .v-live-t-txt,
    .vrhcpt,
    .vrhtt,
    .wpvn_ala_wtt,
    button,
    canvas,
    container-title,
    div > p span,
    h2 > span,
    h2,
    h3,
    img,
    input,
    svg,
    #sw_as {
        filter: grayscale(80%);
    }

    /* Font styles */
    #detailMeta,
    .MicrosoftMap .taskBar > ul li.inputbox input,
    .b_entityTitle,
    .b_searchboxForm,
    .b_topTitle,
    .dest_name,
    .ent-dtab-nam-ov-dtp,
    .hb_title_col,
    .ms-search-tenantLogo-text,
    .qna_algoLink_title,
    .sto_attr_table,
    .sto_attrlitc,
    .vrhtt,
    .wpvn_ala_wtt,
    h4,
    span > h3,
    .b_rc_gb_widget_title {
        font-family: "Segoe UI Variable Text", "Segoe UI", SegoeUI, Segoe,
            Roboto, Arial, sans-serif !important;
    }

    .rd_gtrtb_nav_item {
        font-family: "Segoe UI Variable Small", "Segoe UI", SegoeUI, Segoe,
            Roboto, Arial, sans-serif !important;
    }

	cib-serp,
    #id_n,
    .wr_rat,
    .rwrl,
    .b_primtxt,
    .b_vList,
    a,
    .b_widgetContainer,
    .b_wiki_w_item,
    h2,
    .mc_vtvc_meta_row_channel,
    .mc_vtvc_title,
    .mc_vtvc_title > span,
    .mc_vtvc_title > strong,
    .b_algo > h2 > a,
    .algoSlug_icon,
    .tptxt,
    div > div > div > a,
    span,
    div > p span,
    #fltIdtCon,
    #id_h,
    #vvlb,
    .vrhcpt,
    .metaItem,
    .b_respl,
    .cbtn {
        font-family: var(--roboto-then-segoe-text), Arial, Helvetica, sans-serif !important;
    }

    .b_mgridTitleCont,
    .b_mgridTitle > a,
    .v-ans-title,
    #tob_rail_title,
    .mmtitle > a,
    .l_ecrd_imcolheader_ttl,
    .rs_chatText,
    .df_hd > .b_traits,
    #rd_gtrtb_ttl,
    .annotate .b_focusTextLarge,
    .b_focusTextMedium,
    .b_mgridTitle,
    .b_subModule .b_wfInfoCard .bm_phase2 .ent-dtab-nam-ov-dtnp,
    .irp-video-slogan,
    .nameContainer,
    .sto_title > .sto_slidelr,
    .sto_title,
    .lite-entcard-exp > h2,
    .richrsrailtitle > h2,
    h2 > span,
    p > span > a,
    container-title {
        font-family: var(--segoe-display), Helvetica, Arial, sans-serif !important;
    }

    .ux_ecmp-sel,
    .title,
    .tit,
    .b_title,
    .dirWp,
    .dg_b .infnmpt .infpd a,
    #imagemeta,
    .sb_f_count,
    .b_algospacing,
    .b_bfbLcgAnchorText,
    .b_fbText,
    .b_seeMoreCont,
    .b_vPanel,
    .fdbtext,
    .l_ecrd_ftr_txt,
    .rstc,
    .sto_attr_header,
    .tdw_button,
    .ms-Button-label,
    .ms-search-text-h2,
    .recommendationsTableTitle > h2 {
        font-family: var(--roboto-then-segoe-small), Arial, sans-serif !important;
    }

    .asSectionHeading,
    .sa_tm_subText {
        font-family: var(--roboto-then-segoe-small), Arial, Helvetica,
            sans-serif !important;
    }

    .scopes {
        font-family: var(--roboto-then-segoe-small), Tahoma, Arial, Verdana,
            sans-serif !important;
    }

    .b_smText {
        font-family: var(--roboto-then-segoe-small), Helvetica, sans-serif !important;
    }

    /* Font weights */
    .ent-dtab-nam-ov-dtp,
    .v-ans-title,
    .b_mgridTitle > a,
    #tob_rail_title,
    .mmtitle > a,
    .rs_chatText,
    .df_hd > .b_traits,
    #rd_gtrtb_ttl,
    .annotate .b_focusTextLarge,
    .b_focusTextMedium,
    .b_mgridTitle,
    .b_subModule .b_wfInfoCard .bm_phase2 .ent-dtab-nam-ov-dtnp,
    .irp-video-slogan,
    .nameContainer,
    .sto_title > .sto_slidelr,
    .sto_title,
    .lite-entcard-exp > h2,
    .richrsrailtitle > h2,
    h2 > span,
    container-title {
        font-weight: 600 !important;
    }

    .cCodeBg,
    .rd_code_grn,
    .rd_cCodeBg,
    .rd_code_blu,
    .rd_code_red {
        font-family: var(--mono-combo), Consolas, Courier, monospace !important;
        font-size: 16px !important;
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }
}

@-moz-document domain("geeksforgeeks.org") {
    /* GeeksForGeeks - Code blocks, grayscale */
    div .text > pre,
    .container > div code,
    .outputDiv > pre {
        font-family: var(--mono-combo), Consolas, monospace !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    div.header-main__wrapper,
    div.header-main__slider,
    button.btn.btn-success,
    div.wrapper,
    div.modal.fade,
    div.backdrop,
    footer.gfg-footer,
    div.cookie-consent.hide-consent,
    div.lightbox-target,
    #GFG_AD_Desktop_Stickyunit_1x1 {
        filter: grayscale(80%);
    }
}

@-moz-document domain("forum.xda-developers.com"), domain("xdaforums.com") {
    /* XDADevelopers - Code blocks, grayscale */
    #_qc_cookie_access,
    #pagetual-preload,
    div.menu.menu--wide.menu--right.is-active.is-complete,
    div.menuOutsideClicker,
    div.u-bottomFixer.js-bottomFixTarget,
    div.uix_pageWrapper--fixed,
    span.globalAction,
    #jumpToTop {
        filter: grayscale(80%);
    }

    .node-description,
    .uix_welcomeSection__title,
    #footer,
    .menu-content,
    .p-body,
    .p-nav-inner,
    .p-title-value,
    .bbWrapper,
    .button-text,
    .p-sectionLinks-list,
    .input {
        font-family: var(--not-monospace), -apple-system, BlinkMacSystemFont,
            segoe ui, Roboto, Helvetica, Arial, sans-serif, apple color emoji,
            segoe ui emoji, segoe ui symbol !important;
    }

    .bbCodeInline,
    .bbCodeCode {
        font-family: var(--mono-combo), Monaco, Menlo, Consolas, roboto mono,
            andale mono, ubuntu mono, monospace !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }
}

@-moz-document domain("discord.com") {
    /* Discord - Change font and formatting of code blocks, add fallbacks */
    .card-3ie2dy > div div,
    .constrainedRow-3y91Xf,
    .container-1eFtFS,
    .defaultColor-1EVLSt,
    .headerText-10ez_d,
    .standardSidebarView-E9Pc3j,
    .text-sm-normal-AEQz4v,
    .text-xs-normal-3O7EaX,
    button,
    h2,
    h3,
    span,
    input,
    .message-wB3GWa,
    .text-md-semibold-2VMhBr {
        font-family: "gg sans", "Noto Sans", "Noto Sans JP", "Noto Sans TC",
            "Noto Sans SC", "Noto Sans KR", "Helvetica Neue", Helvetica, Arial,
            sans-serif !important;
    }

    .hljs,
    .codeLine-2C-9aH,
    .inlineCode-ERyvy_,
    table,
    .inline {
        font-family: var(--block-mono), var(--monospace), Consolas,
            "Andale Mono WT", "Andale Mono", "Lucida Console",
            "Lucida Sans Typewriter", "Liberation Mono", "Nimbus Mono L", Monaco,
            "Courier New", Courier, monospace !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    .roleMention-11Aaqi,
    .membersGroup-2eiWxl,
    .roles-3vQPxb,
    .username-3_PJ5r,
    .username-h_Y3Us,
    .username-vAneIW,
    .mention {
        filter: grayscale(80%);
    }
}

@-moz-document domain("knowyourmeme.com") {
    /* KnowYourMeme - Grayscale, font */
    .enlargeGalleryProgress,
    #content,
    #maru,
    #mast_links,
    #omgkym,
    #photo,
    #search_box,
    #section_header,
    .home,
    .sidebar_box,
    .tc,
    .welcome,
    .bodycopy,
    a,
    aside,
    em,
    h1,
    h2,
    h3,
    h5,
    h6,
    li,
    p > strong,
    p,
    div > h4 {
        font-family: var(--font-personal), Helvetica, Arial, Verdana, sans-serif !important;
    }

    /* monospace */
    textarea {
        font-family: var(--block-mono), var(--monospace), "monaco",
            "lucida console", "lucida sans typewriter", "consolas", "monospace" !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    /* grayscale */
    #cboxOverlay,
    #container,
    #footer,
    #split-test,
    #sub_network_footer_wrapper,
    .r-top-block,
    div.header-unit-wrapper,
    div.hide,
    div.ui-helper-hidden-accessible,
    div.x_brb6.c,
    img,
    ul.ui-menu.ui-widget.ui-widget-content.ui-autocomplete.ui-front,
    #pagetual-preload {
        filter: grayscale(80%);
    }
}

@-moz-document domain("codeberg.org") {
    /* Codeberg - Fonts, grayscale */
    .height,
    .ui,
    .file-view,
    h1,
    h2,
    h3,
    .grid,
    .gt-df {
        font-family: var(--font-personal), -apple-system, "Segoe UI", system-ui,
            Roboto, "Helvetica Neue", Arial, "Noto Sans", "Liberation Sans",
            sans-serif, "Apple Color Emoji", "Segoe UI Emoji",
            "Noto Color Emoji", "Twemoji Mozilla" !important;
    }

    code {
        font-family: var(--mono-combo), ui-monospace, SFMono-Regular, "SF Mono",
            Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace,
            "Apple Color Emoji", "Segoe UI Emoji", "Noto Color Emoji",
            "Twemoji Mozilla" !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    div.full.height,
    footer.page-footer {
        filter: grayscale(80%);
    }
}

@-moz-document domain("docs.oracle.com") {
    /* Oracle docs - Fonts, grayscale */
    pre,
    dd,
    code {
        font-family: var(--mono-combo), "DejaVu Sans Mono", monospace !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    div.bottomNav,
    div.contentContainer,
    div.header,
    div.subNav,
    div.topNav,
    p.legalCopy,
    #footer-banner {
        filter: grayscale(80%);
    }
}

@-moz-document domain("yahoo.com") {
    /* Yahoo - Fonts, grayscale */
    #doc {
        font-family: YahooSans VF, Roboto, helvetica neue, helvetica, arial,
            sans-serif;
    }

    .code-snippet {
        font-family: var(--mono-combo), "SourceCodePro" !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    .snippet-row {
        font-family: var(--mono-combo), CourierNewPSMT !important;
        font-size: 16px !important;

        /* Ligatures */
        font-variant-ligatures: common-ligatures, contextual !important;
        font-feature-settings: "liga" 1, "calt" 1 !important;
    }

    #HPSPON-ad,
    #Header,
    #Masterwrap,
    #advertisement-thamba,
    #doc,
    #pagetual-preload,
    #sa-feedback-panel,
    #yui3-css-stamp,
    div.darla,
    div.hide,
    div.sa-aria-live-region,
    div.uh3-p,
    div.uh3-p.uh3lite,
    div.wafer-lightbox-overlay,
    div.wafer-template,
    div.yui3-widget.yui3-safesearch.hide,
    div._yb_1ayze._yb_2ahyx {
        filter: grayscale(80%);
    }
}